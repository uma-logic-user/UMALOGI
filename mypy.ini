[mypy]
python_version = 3.11
# Suppress missing-stub errors for third-party libs (pandas / sklearn / lightgbm etc.)
ignore_missing_imports = True
follow_imports = silent
warn_unused_ignores = False
no_implicit_optional = True
# Core modules are gradually hardened toward strict typing (type-safety foundation).
check_untyped_defs = True
